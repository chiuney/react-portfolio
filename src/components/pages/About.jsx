import styled from "styled-components";

export const About = () => {
  return (
    <SContainer>
      <h1>ABOUT ME</h1>
    </SContainer>
  );
}

const SContainer = styled.div`
  text-align: center;
`