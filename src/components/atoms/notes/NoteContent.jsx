import styled from "styled-components";
import media from "styled-media-query";

export const NoteContents = (props) => {
  const { showContent , setShowContent , content, index, notes ,key } = props;

  return (
    <>
      {/* {showContent ?
        (<p>aaa</p>)
        :
        (<></>)
      }
      {
        console.log(index),
        console.log(notes),
        console.log(key)
      } */}
    </>
  );
}